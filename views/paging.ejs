<br />
<div>
  <% if (!isFirstPage) { %>
    <span class="prepage"><a title="next" href="?p=<%= page-1 %>">上一页</a></span>
  <% } %>

  <% if (!isLastPage) { %>
    <span class="nextpage"><a title="pre" href="?p=<%= page+1 %>">下一页</a></span>
  <% } %>
</div>